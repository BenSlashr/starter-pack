---
import NewsletterForm from './NewsletterForm.tsx';
---

<section id="newsletter" class="py-section relative overflow-hidden">
  <div class="absolute inset-0 bg-gradient-to-r from-primary/5 via-secondary/5 to-primary/5"></div>
  <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[600px] h-[600px] bg-primary/5 rounded-full blur-[120px] pointer-events-none"></div>
  <div class="absolute bottom-0 right-1/4 w-[400px] h-[400px] bg-secondary/5 rounded-full blur-[100px] pointer-events-none"></div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="max-w-2xl mx-auto text-center">
      <div class="inline-flex items-center gap-2 px-4 py-1.5 mb-6 text-xs font-medium bg-primary/10 text-primary border border-primary/20 rounded-full">
        <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
        </svg>
        Newsletter gratuite
      </div>

      <!-- TODO: Replace with your newsletter headline -->
      <h2 class="font-display font-800 text-display leading-[1.1] mb-4 tracking-tight">
        L'essentiel,<br />
        <span class="text-gradient-primary">chaque semaine</span>
      </h2>

      <p class="text-body-lg text-text-secondary mb-8 max-w-lg mx-auto">
        Recevez notre selection hebdomadaire. Zero spam, 100% valeur.
      </p>

      <div class="max-w-md mx-auto">
        <NewsletterForm client:visible layout="inline" />
      </div>

      <p class="text-xs text-text-muted mt-4">
        Desabonnement en un clic. Vos donnees restent privees.
      </p>
    </div>
  </div>
</section>
