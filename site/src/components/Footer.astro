---
import Logo from './Logo.astro';

// TODO: Replace with your footer links
const footerLinks = {
  'Guides': [
    { label: 'Bien demarrer', href: '/guides/getting-started' },
    { label: 'Avance', href: '/guides/advanced' },
  ],
  'Outils': [
    { label: 'Tous les outils', href: '/outils' },
    { label: 'Avis', href: '/avis' },
    { label: 'Comparatifs', href: '/comparer' },
    { label: 'Glossaire', href: '/glossaire' },
  ],
  'A propos': [
    { label: 'Notre equipe', href: '/a-propos' },
    { label: 'Contact', href: '/contact' },
    { label: 'Newsletter', href: '/newsletter' },
    { label: 'CGU', href: '/cgu' },
    { label: 'Confidentialite', href: '/confidentialite' },
  ],
};

// TODO: Replace with your real social links (uncomment and add URLs)
// Dead "#" links waste crawl budget and confuse users — only include real URLs
const socials: Array<{ name: string; href: string; icon: string }> = [
  // { name: 'Twitter / X', href: 'https://twitter.com/yourhandle', icon: 'M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z' },
  // { name: 'YouTube', href: 'https://youtube.com/@yourchannel', icon: 'M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z' },
];
---

<footer class="border-t border-border bg-abyss">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="py-16 grid grid-cols-2 md:grid-cols-4 gap-10">
      <!-- Brand -->
      <div class="col-span-2 md:col-span-1">
        <a href="/" class="inline-block mb-4 cursor-pointer group">
          <Logo variant="horizontal" size="sm" />
        </a>
        <!-- TODO: Replace with your site description -->
        <p class="text-sm text-text-secondary leading-relaxed mb-6 max-w-xs">
          Description de votre site. Guides, outils et ressources pour votre audience.
        </p>
        <div class="flex items-center gap-3">
          {socials.map((social) => (
            <a
              href={social.href}
              class="w-9 h-9 rounded-lg border border-border hover:border-text-muted bg-surface-raised/50 flex items-center justify-center transition-colors duration-200 cursor-pointer hover:bg-surface-hover"
              aria-label={social.name}
            >
              <svg class="w-4 h-4 text-text-secondary" fill="currentColor" viewBox="0 0 24 24">
                <path d={social.icon} />
              </svg>
            </a>
          ))}
        </div>
      </div>

      {Object.entries(footerLinks).map(([title, links]) => (
        <div>
          <h4 class="font-display font-700 text-sm mb-4 text-text-primary">{title}</h4>
          <ul class="space-y-2.5">
            {links.map((link) => (
              <li>
                <a href={link.href} class="text-sm text-text-secondary hover:text-primary transition-colors duration-200 cursor-pointer">
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>

    <div class="py-6 border-t border-border flex items-center justify-center">
      <!-- TODO: Replace with your copyright -->
      <p class="text-xs text-text-muted">
        &copy; {new Date().getFullYear()} MySite. Tous droits reserves.
      </p>
    </div>
  </div>
</footer>
